# Build S01E04 The Burrow

- **Piece:** `s01e04Burrow`
- **Launched:** 2026-09-24 17:08 UTC · **model:** claude-opus-5-5[1m]
- **Cost:** 213,754 tokens · 47 tool calls · 9.7 min
- **Source:** `6f834591-5513-4f19-a058-1af9a5cdd8f7.jsonl` (extracted by `tools/extract-runs.mjs`)

## Prompt

````markdown
Your complete instructions are in ~/rotli-studio/motion/workflows/prompts/s01e04.prompt.md. Read that file first and follow it exactly: it is the contract, generated from the episode brief. Do not edit shared files. When done, report exactly what it asks for.
````

## Report (the agent's own summary, unedited)

Episode s01e04 "The Burrow" is built and passes all the checks in the brief: the render shows no dead air, the type check is clean for s01e04, and I looked at every sheet. Nothing is committed, and no shared file was edited apart from the `add-story` registration in `pieces.json`.

**Files created**
- `~/rotli-studio/motion/src/canvas-core/s01e04Burrow.ts`: the episode and `s01e04Derive`. Every helper is defined locally in this file.
- `~/rotli-studio/motion/src/hosts/page-s01e04Burrow.ts`: from the scaffold.
- `s01e04Vertical.ts`, `s01e04Carousel.ts`, `s01e04Single.ts` and their `page-*` hosts: generated by `derive-modules.mjs s01e04Burrow "The burrow." burrow`.
- `add-story.mjs` registered the episode and its 3 derivatives in `pieces.json`.

**The episode:** grove-burrow atmosphere, flat style, music key 7 / melody 0. I retimed two scenes: remove went from 360 to 405 frames and storm from 450 to 405, so the three captions in remove each stay on long enough to read. All lengths are still multiples of 15 and sum to 1800.
- **cold (240):** wind blows the papers out of the hut. The quokka hops after them, catches the Trip ideas card, and thinks a folder.
- **chapter (120):** "The burrow.", pose `stays_local`.
- **dig (390):** the burrow drawn as a folder tree. `Island/` opens with `notes/`, `chats/`, `storage/` and a dashed `.rotli/` labelled "rebuildable" with a turning arrow. Trip ideas travels down the tunnel into `notes/`.
- **remove (405):** the app window, zoomed so text is at least 26px on screen.
  - Click Files, then Remove from Main on Ferry times: it leaves the sidebar but stays in `notes/`.
  - Delete Call Sam: it moves under `trash/`, and an "Empty Trash" button appears that nobody clicks.
- **storm (405):** intertitle "Then the storm came.", then the storm takes the hut's roof and walls. Below ground the burrow stays lit and calm. The quokka climbs out and celebrates in the dashed outline where the hut stood.
- **end (240):** intertitle "The hut blew away. / Every file stayed." with the Empty Trash claim underneath, then the end card, "Next: The Lighthouse Keeper".

All five brief claims appear word for word as captions; three are split across a headline and a second line. The derive spec has 4 vertical beats (900 frames total), 4 slides and 1 single, with every crop's aspect between 0.9 and 1.8.

**Final sheets**
- `/tmp/s01e04.png` (the main film)
- `/tmp/s01e04v.png` (vertical)
- `/tmp/s01e04k.png` (carousel)
- `/tmp/s01e04s/f0014.png` (single)

**Still-frames:** `/tmp/s01e04.mp4` printed `identical: 0` and an empty `windows:` line.

**Claims and names I was unsure of**
- The folder names in the diagram and the Files list (`Island/`, `notes/`, `chats/`, `storage/`, `trash/`, `.rotli/`) and the example files ("Trip chat.md", "lake.jpg", "ferry.pdf") are made up. `trash/` follows the `trash/storage/...` path in the vault contract; the others follow the brief. They may not match the app's real folder layout.
- The context menu items "Open / Remove from Main / Delete" and the "in Main" labels in the Files list are drawn by me, not copied from the app's UI.
- The short titles in the derivatives are paraphrases, not claim text. For example: "Plain Markdown, in a folder you choose.", "Remove from Main keeps it in your vault.", "Trash lives inside the vault.", "The work still makes sense."
- To look stormy, the sky above ground is darkened with a flat translucent layer of the family's night colour.

**What didn't fully work**
- As the vertical cut slowly zooms in, the beat 2 crop trims the edge of the `storage/` room and the beat 4 crop trims `.rotli/`. These are minor.
- The `rest` pose shows a standing quokka, not a sleeping one, so during the storm it reads as calm rather than asleep.
- The sound still needs a person to listen once.
