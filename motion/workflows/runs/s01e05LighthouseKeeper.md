# Build S01E05 Lighthouse Keeper

- **Piece:** `s01e05LighthouseKeeper`
- **Launched:** 2026-09-24 17:08 UTC · **model:** claude-opus-5-5[1m]
- **Cost:** 228,372 tokens · 49 tool calls · 9.9 min
- **Source:** `6f834591-5513-4f19-a058-1af9a5cdd8f7.jsonl` (extracted by `tools/extract-runs.mjs`)

## Prompt

````markdown
Your complete instructions are in ~/rotli-studio/motion/workflows/prompts/s01e05.prompt.md. Read that file first and follow it exactly: it is the contract, generated from the episode brief. Do not edit shared files. When done, report exactly what it asks for.
````

## Report (the agent's own summary, unedited)

The episode is finished and every required check passes: the still-frames `windows:` line is empty and `tsc` prints nothing for s01e05. Nothing was committed, and I didn't touch `~/rotli` or any shared file.

**Files**
- Created: `~/rotli-studio/motion/src/canvas-core/s01e05LighthouseKeeper.ts` and `~/rotli-studio/motion/src/hosts/page-s01e05LighthouseKeeper.ts` (from the scaffold).
- Generated by `derive-modules.mjs`: `s01e05Vertical.ts`, `s01e05Carousel.ts`, `s01e05Single.ts` and their three host pages.
- `add-story.mjs` added the episode and its three derivatives to `pieces.json`.

**The six scenes (1800 frames, same order and lengths as the brief)**
1. **Cold open (240, night):** "Night falls." The quokka hops in carrying six Captures on its back, with a "Captures 6" pill. It yawns (the rest pose plus a stretch), thinks of the moon, sets the notes down and falls asleep.
2. **Chapter card (120).**
3. **Keeper (300):** intertitle "While it sleeps, someone files the day away." The Librarian pops out of the lighthouse door, gathers the pile, and climbs a ladder rung by rung to a balcony. The lamp brightens at the top.
4. **Filing (480):** six notes fly to People, Projects and Research, and each gets a tag. Links are drawn between related notes. The Trip ideas card opens up to show its area, summary, tags, links and the "Recorded … ↶ undo" line. An on-device chip with a face sits beside the Librarian.
5. **Morning (420, visits `linen-morning`):** the app window shows the Trip ideas note with its tags, area and summary, the words outlined with a "words unchanged" badge, and the Library counts in the sidebar. A "Librarian · last night" panel lists the moves. The pointer clicks Undo on Salt lakes: "✓ unchanged since it was filed" appears and the note goes back to Captures. A "New vault" panel then picks "Raw vault · no AI". The quokka wakes up with sun and check thoughts.
6. **End (240):** "Filed by morning. / Every word, as written.", then the end card with "Next: Threads".

**Checks**
- Sheets: `/tmp/s01e05.png`, `/tmp/s01e05v.png` (vertical), `/tmp/s01e05c.png` (carousel), `/tmp/s01e05s.png` (single).
- Render: `/tmp/s01e05.mp4`. `still-frames` printed `identical: 0` and an empty `windows:` line.

**Claims I was unsure of**
- Claim 1 is used in full in the filing scene, split over the caption's two lines. In the morning scene I shortened it to "The Librarian records what it did / with a guarded undo." by dropping the middle of the sentence. It's shorter, not extended, but please confirm that kind of cut is allowed.
- The on-screen demo showing the undo guard as "unchanged since it was filed" is my reading of "guarded undo". The repo's docs describe undo as checking the note hasn't changed since it was filed.
- The "New vault" panel with the options "With the Librarian" and "Raw vault · no AI" is invented UI that illustrates claim 4. It isn't copied from the real app.
- The derivative titles are my own story lines, not feature claims (as in E01): "Filed by lighthouse light.", "Any move can be undone.", "It records what it did."

**What didn't fully work**
Nothing is broken, but three things are worth your eye:
- The night atmosphere draws its own faint lighthouse at the right edge. In the keeper scene my big lighthouse sits over it. In the filing scene it shows just behind the Librarian's shoulder.
- The thought bubbles are low-contrast on the dark background. That's the shared template's night styling.
- The single card's crop has some empty dark space at the bottom, left there to keep the Librarian's feet in frame.

The sound hasn't been listened to.
