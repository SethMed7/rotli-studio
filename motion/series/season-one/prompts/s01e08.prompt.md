You are building ONE episode of "Rotli · Season One", a series of code-drawn one-minute story films. Work ONLY in
~/rotli-studio/motion (a Node/npm project; use `node`, not bun; `~` is your home directory, expand it to an
absolute path for file tools). In every shell command use absolute
paths or `cd ~/rotli-studio/motion && …` (the shell's cwd resets between commands).

YOUR EPISODE: `s01e08LockedBox`, brief at `series/season-one/episodes/s01e08.json` (read it first; it is the contract).

REQUIRED READING, in order:
1. `series/season-one/bible.md` covers the world, the cast, the MUTE quokka rule, atmospheres, styles and the episode shape.
2. `src/canvas-core/s01e01Arrival.ts` is THE reference episode. Match its structure: scenes array → `story({...})`, `sceneStart`,
   chapterCard/intertitle/thought/storyEnd, lowerThird feature captions, the derive spec with label "Rotli · Season One · NN".
3. `src/canvas-core/studio/story.ts` (the engine + templates), `studio/atmospheres.ts`, `studio/ui.ts` (appFrame, task, results,
   choice, table, codeBlock, link, tagChip, fileList, pointer, keys, raw, caret), `studio/series.ts` (lowerThird, host, drift),
   `studio/derive.ts`, `rotli/kit.ts` (card, chip, folder, cloud, lockIcon, fillRR, text, ink, measure),
   `rotli/island.ts` (ferry, jetty, sign, bike, lighthouse), `rotli/actor.ts` (actor, hopAlong, poseAt).
4. Pose names for `actor`: base, waving, notes, celebrating, ai_chat, searching, knowledge_system, stays_local, inbox, rest, walking,
   thoughtful, listening, attention. Do NOT read quokka/poses.ts (it is huge path data).
5. `../.claude/skills/motion-room/SKILL.md`.

HARD RULES:
1. Scaffold first: `node tools/new-episode.mjs series/season-one/episodes/s01e08.json` (it writes a runnable stub + host). Then replace each
   stub scene's draw with the real scene. Create/modify ONLY `src/canvas-core/s01e08LockedBox.ts` and `src/hosts/page-s01e08LockedBox.ts`. When the
   episode is done, run `node tools/derive-modules.mjs s01e08LockedBox "The locked box." box` and `node tools/add-story.mjs series/season-one/episodes/s01e08.json`.
   Do NOT edit any shared file (studio/*, rotli/*, quokka/*, tools, pieces.json beyond add-story, goldens). If you need a helper,
   define it locally in your episode file.
2. Story law: follow the brief's setup → turn → payoff and scene list (you may retime scenes; lengths must stay multiples of 15 and
   sum to 1800). The quokka NEVER speaks words: it feels and intends through `thought(..., glyph)` pictograms; the story's words are
   `intertitle` cards; feature claims are `lowerThird` captions using ONLY the brief's `features[].claim` wording (shortened is fine,
   never extended). The recurring token is the Trip ideas card (`card({ token: true, title: "Trip ideas" })`) where it fits.
3. Look law: the brief's atmosphere (scenes may visit others via `atm`), theme colours only via `C` / `LIGHT` / `DARK` (the engine
   applies the family per scene), the site's grounds (never stripes), flat marks (no glows/gradients/blur), nothing overlaps
   unintentionally, captions never cover what they describe, UI body text ≥ 26px on screen (push the camera in if needed),
   the brief's style (`line` = `lineQuokka` + outline props, and pass `line: true` to chapterCard/storyEnd; `paper`/`flat` = filled). The Librarian is `librarian(...)` only.
4. Every scene keeps moving (ambient + drift are automatic; add sway/hops/pointer motion in holds).
5. The derive spec: 4 vertical beats (each len a multiple of 15, total ≤ 900), 4 slides at settled frames, 1 single; crops in
   rendered screen space, aspect between 0.9 and 1.8. Lower thirds are hidden automatically inside derivatives, so crops may
   include the bottom of the frame (feet, props).
6. VERIFY BY LOOKING, and iterate until right:
   - `node tools/frames.mjs s01e08LockedBox 40,200,400,600,800,1000,1200,1400,1600,1760 --out /tmp/s01e08 --sheet /tmp/s01e08.png --cols 5` then READ the PNG
   - the same for `s01e08Vertical`, `s01e08Carousel` (frames 14,29,44,…) and `s01e08Single` (frame 14)
   - `node tools/render.mjs s01e08LockedBox --out /tmp/s01e08.mp4` then `node tools/still-frames.mjs /tmp/s01e08.mp4` must print an EMPTY `windows:` line
   - `npx tsc --noEmit -p tsconfig.json 2>&1 | grep s01e08` must print nothing
7. Do not commit, do not touch ~/rotli, do not run `studio.mjs render all` or golden.

Report back: files created, the final sheet paths, the still-frames result, any claim you were unsure of, and anything you could
not make work.

--- THE EPISODE (from series/season-one/episodes/s01e08.json) ---
Title card: ["The locked box."] · Season One · 08 · next: Other Shores
Logline: Some things stay in the house, whoever knocks.
Setup: Rain at night; the quokka keeps its passport note somewhere safe.
Turn: It marks the note secure: remote clouds knock and are turned away at the door; the on-device friend may read it. A pasted secret is caught on its own. A locked note can be read by every model and edited by none.
Payoff: Warm inside. The weather stays outside.
Token: the Trip ideas note. Cast: the quokka, the clouds (remote models), the chip (on-device model).
Atmosphere: midnight-rain · style: line · music: {"key":2,"melody":0}
Feature claims (the ONLY wording allowed in captions):
  - "Mark a note secure and Rotli moves it to wiki/_secure/ and keeps it away from remote models and web lookups; the Librarian leaves it alone." [README.md]
  - "Rotli also recognizes common secret shapes (API keys, private keys, card and identity numbers) and treats them the same way." [README.md]
  - "Remote models never see secure content; on-device models can, unless you turn that off." [README.md]
  - "Want AI to stop touching a note? Lock it. Every model can still read it; none can edit it." [rotli.co /features]
Scenes (id · frames · what):
  - cold · 240 · story: rain, line-art quokka at the window, thought: lock
  - chapter · 120 · chapterCard
  - secure · 420 · story: clouds knock, turned away; chip reads
  - shapes · 330 · appDemo: a fake key is caught
  - lock · 330 · appDemo: locked note refuses an edit
  - payoff · 240 · intertitle
  - end · 120 · storyEnd

