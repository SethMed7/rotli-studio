You are reviewing the rotli studio repository in the current directory (read-only: do NOT modify, create or delete any file, and do not run anything that writes). It is a public, MIT open-source "studio" for Rotli (a local-first Mac notes app): motion/ (code-drawn films: an anidoodle-derived engine, pieces, series, goldens, tools), sound/ (music and effects composed in code), a Bun server (server.ts, src/motion/*, static/*) that serves a read-only "Motion room" site plus a local content editor (/create), scripts/ (export-site.ts builds a static snapshot hosted at studio.rotli.co; check-public.ts, check-isolation.ts, link-post.ts, make-favicon.ts), .claude/skills, workflows and prompts, DESIGN.md, AGENTS.md, README.md. Skip node_modules, out/, site-dist/, dist/.

Write your review as Markdown: a 3-sentence summary; then findings ordered by severity (critical / high / medium / low), each with a short title, the file path and line(s), what is wrong and why it matters, and a concrete suggested fix; then "What is good" (brief); then "Top 5 changes I would make first". Be specific and verifiable: quote code or cite lines. Do not invent files. Your perspective:

SECURITY, PRIVACY AND OPEN-SOURCE HYGIENE. The repo and a static snapshot of it are public. Look for: secrets or personal data (home paths, emails, private product or client names, vault content), anything in the snapshot that should not be public, path traversal or unsafe file serving in server.ts / src/motion/routes.ts, command injection in spawned processes (ffmpeg, node, bun), the static host config (deploy/Caddyfile: headers, caching, CSP), third-party licence compliance (motion/third_party/anidoodle, fonts in library/fonts and motion/assets), the owner-only posting path (scripts/link-post.ts, motion/posts.json), the pre-push gate (scripts/check-public.ts, .githooks) and whether it can be bypassed or misses cases, and supply-chain risks (dependencies, lockfiles).
